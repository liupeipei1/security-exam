spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:exam-db-dev}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: ${DB_USER:peipei}
    password: ${DB_PASSWORD:298280}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  data:
    redis:
      port: 6379
      password: ${REDIS_PASSWORD:}
      host: ${REDIS_HOST:localhost}

jwt:
  secret: ${JWT_SECRET:exam-secret-key-2024}
  expiration: 86400000

wechat:
  app-id: ${WECHAT_APPID:wx782c26e4c19acffb}
  app-secret: ${WECHAT_APPSECRET:8529145e62054e92607c5c485b35d052}
  pay:
    enabled: ${WECHAT_PAY_ENABLED:false}
    mch-id: ${WECHAT_PAY_MCH_ID:1603540461}
    mch-key: ${WECHAT_PAY_API_KEY:2D8F6E3A9B5C7D1E4F8A2C9B6D3E7F1A}
    notify-url: ${WECHAT_PAY_NOTIFY_URL:http://localhost:8080/api/pay/notify}
