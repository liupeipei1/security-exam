server:
  port: 8085

spring:
  application:
    name: exam-user-center-service
  datasource:
    url: jdbc:mysql://localhost:3306/exam-db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: ${DB_USER:peipei}
    password: ${DB_PASSWORD:298280}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  redis:
    host: localhost
    port: 6379
    password:

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/