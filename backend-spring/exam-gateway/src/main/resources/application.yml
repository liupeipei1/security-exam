server:
  port: 8080

exam:
  jwt:
    secret: ${JWT_SECRET:change-me-exam-jwt-secret-key-32bytes-min}
    expiration-hours: ${JWT_EXPIRATION_HOURS:168}

spring:
  application:
    name: exam-gateway
  cloud:
    gateway:
      default-filters:
        - name: JwtAuth
      discovery:
        locator:
          enabled: false
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        - id: auth-service
          uri: lb://exam-auth-service
          predicates:
            - Path=/api/auth/**
        - id: user-service
          uri: lb://exam-user-service
          predicates:
            - Path=/api/user/**
        - id: question-service
          uri: lb://exam-question-service
          predicates:
            - Path=/api/banks/**,/api/questions/**,/api/question/**,/api/guide/**,/api/knowledge/**,/api/notes/**,/api/speech/**
        - id: exam-service
          uri: lb://exam-exam-service
          predicates:
            - Path=/api/exam/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
